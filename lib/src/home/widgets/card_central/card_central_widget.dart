import 'package:flutter/material.dart';

class CardCentralWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      margin: EdgeInsets.all(20),
      color: Colors.white,
    );
  }
}
